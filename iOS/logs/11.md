2026-06-10 10:25:24 EDT

Implemented xTool F1 discovery on the current Wi-Fi subnet, including endpoint reuse in the app and CLI fallback discovery when `--host` is omitted.

Fixed machine-info parsing for the actual F1 response shape returned by `/device/machineInfo`, including wrapped `data` payloads and observed LG4/10W+2W metadata.

Added focused discovery tests and on-device diagnostics for failed scans. Added a hidden launch-triggered connection check so the installed iOS app path can be verified through the phone app container log.

Updated project instructions to forbid reporting iOS feature success unless the installed app path was actually verified on-device.
