2026-06-10 15:34:51 EDT

Implemented the canvas/settings UI refinement, reusable copied settings presets, shared deduped image library, Library tab, and soft alignment snapping.

Fixed the launch crash caused by Swift exclusivity during legacy store migration by avoiding nested mutation of project photos while creating shared assets.

Verified with `swift build`, local migration probe against copied iPhone data, simulator launch, and iPhone install/launch.
