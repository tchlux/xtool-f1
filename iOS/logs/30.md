2026-06-14 21:41:16 EDT

Documented the healthy tiered test protocol in the README, added the `test.sh` tier entry point, and updated simulator/phone launch scripts to share named app scenarios. Refactored app launch argument handling into scenario parsing, added deterministic preview and first-project launch states, split the core tests into concern-based classes, and removed the SwiftPM asset catalog warning.

Verified with `./test.sh quick`, `./test.sh core`, simulator `editor-smoke` and `preview-project` launches, and `./run-phone.sh` install/launch.
