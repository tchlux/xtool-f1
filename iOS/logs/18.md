2026-06-11 12:00:18 EDT

Implemented print preview fixes:

- Generate transparent, power-calibrated raster previews instead of a binary black-on-filled background.
- Render a blue active scan row masked to printable preview pixels.
- Keep dropped pixels out of preview output and preserve multi-raster compositing.
- Persist per-asset photo editor undo/redo history and refresh preview after project/asset undo changes.
- Remove unselected raster image borders and keep selected borders visually constant under canvas zoom.

Verified with `swift test`, simulator screenshots, and `./run-phone.sh` build/install/launch.
